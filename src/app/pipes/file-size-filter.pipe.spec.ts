import { FileSizeFilterPipe } from './file-size-filter.pipe';

describe('FileSizeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FileSizeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
